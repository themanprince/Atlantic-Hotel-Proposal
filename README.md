### SETUP INSTRUCTIONS
* run `pip install requirements.txt`
* set the variable DATABASE_URL in the .env file
* run `python -m alembic upgrade head`
* view customer home at "/"
* view admin dashboard at "/admin/dashboard"

